spring:
  datasource:
    url: jdbc:postgresql://localhost:3306/staemdb
    username: postgres
    password: mariobrosluigi
  jpa:
    database: postgresql
    show-sql: true
    hibernate:
      ddl-auto: create-drop
